// Wagmi configuration - simplified to avoid API compatibility issues
export const wagmiConfig = {
  // Wagmi configuration would go here
  // For now, just export a basic config to avoid build errors
  chains: [],
  providers: []
}

export const getWagmiConfig = () => wagmiConfig
